# RITME-Velorn
RITME Pipeline integrated into Velorn video editor.

## Setup
1. Python backend: cd ritme_backend && pip install -r requirements.txt
2. cp .env.example .env and edit API keys
3. cd .. && npm install && npm run dev

